{
  "extends": "next/core-web-vitals",
  "rules": {
    "semi": ["error", "never"],
    "no-extra-semi": "error",
    "quotes": ["error", "single"]
  },
  "overrides": [
    {
      "files": ["*.ts"],
      "rules": {
        "semi": ["error", "never"]
      }
    }
  ]
}
